# PLANETAI — eight prompts to build the dashboard the sketch shows

*Written 20 September 2026 from the sketch at https://claude.ai/artifact/XK77UAbSNTjdFjqkyhvs8Y (rounds 1–4, 18–20 Sep) and the notes `PLANETAI_Dashboard_Sketches_Simple_Advanced_2026-09-18.md`, `PLANETAI_Motion_and_Loading_2026-09-20.md` and the data contract `PLANETAI_Dashboard_Data_Contract_2026-09-20.md` (amended 20 Sep evening against v0.64, then again the same night against v0.66). This pack replaces Phase 1 of `PROMPT_dashboard_redesign_2026-09-14.md` (the three directions): the pick is made, it is the sketch. Phase 2 of that prompt is cut here into surfaces so each can run in one session. The 14 Sep targets T1–T9, its extension contract (`ui:` blocks, `SLOTS`, `ANATOMY`), its gates and its hard rules all stand; they are not repeated in full, they are cited.*

*Each prompt is paste-ready for a Claude Code session on **Opus** in `~/Documents/Claude/Projects/FAB CITY/planetai-node` on the branch `dashboard-redesign-2026-09` (the working checkout; `main` is the beta channel and node #1 and node #2 update from it). Prompt 0 and the DESIGN.md half of prompt 6 ship under Tomas's name and go to **Fable**. Run in order; every prompt shares `app/static/`, so a second session starts after the first has committed. Nothing reaches testers before Tomas looks (R9); nothing here tags a release except prompt 7, and only when he says so.*

*Standing facts, verified 20 Sep against `main` at `d01330c` (**v0.66**; nine releases since the sketch began — v0.58 polling and `stale`, v0.59 the dial on Now only and the headline ranked by state then by what moved, v0.60 signed updates, v0.61 the registry inside the node and the packs `season` · `xiaomi-air` · `thingdata`, v0.62 gates, v0.63 `schema` keys and `AGENT_PREFER=private`, v0.64 `adapter` strings, **v0.65** the registry re-pinned to `85a194c` with 217 entries of which eight carry `role: act` (`act_kind` facility · design · network) and a new code pack `make` that stores the active fab labs within `MAKE_RADIUS_KM` as `sensors` rows of `kind='facility'` — places, never metrics, never a cell, never an alert — and ends the report with one line, "Nearest place to make or fix something: …", **v0.66** the pack behind its own switch `MAKE_ENABLED=0` because the Fab Lab Network directory is not openly licensed and the Foundation's decision to read it covers the Foundation, not a node's keeper): the page is three static files (`index.html`, `dashboard.js`, `dashboard.css`) plus `tokens.css`, `planetai-theme.css`, `signs.svg`, `node-ground.svg`, `kilometre-cells.json` and self-hosted fonts, served from `/static/`; no framework, no build step, no CDN, offline on a LAN; four stages registered in `dashboard.js` (observe · decide · act · measure); `GET /issues` is the page's main read; the frozen language is `planetai-design/references/planetai-layer.md` and `planetai-theme.css` is generated from it and diffed by `check_theme.py`; gates are `make lint && make test`; docs are `docs/site/*.md`, built by `tools/build_docs.py` to `planetai.fab.city/docs/` — the node does not serve them itself; node #1 `bayu-ungasan` is evidence, never a test bench; fixture for every render is a `planetai snapshot` of node #1 taken 18 Sep 21:24 WITA, committed under `docs/design/fixtures/`. **Every field a component reads is named in `PLANETAI_Dashboard_Data_Contract_2026-09-20.md`; a session that wants a field not on that page's wire STOPs and lists it rather than computing it in the browser.***

*Decisions taken by Tomas that these prompts assume, so no session relitigates them: the plate is paper by day, dark on the wall and by a keeper's switch; the resolution rail is the top instrument; the page follows the four stages; three modes (simple · advanced · learn) as a page setting; the learn layer quotes `docs/site` verbatim; the satellite loop leads Historical and the Sentinel frames come from `/earth`, never a live tile; the glyph globe is the loading state and plays on first paint, on ↻ and on reconnect after `stale`; the layer is published as a DESIGN.md; React may live on planetai.fab.city and never in `app/static/`. Still open, and each prompt names where it asks: mono versus Funnel for the monument numeral (default here: mono, behind a token); dotted texture versus cells-blue for the rail's leave-the-machine stops (default here: dotted, and `docs/site/dashboard.md` changes to match); the five new signs (in, after a 12 px contact sheet); **one vocabulary for the four distances** (default here: `LABEL_WORDS` becomes the sentence nouns — room · wall outside · street · model, with `id`/`es` to match — so a heading and the sentence under it agree; the API keys `room/yard/ring/region` do not change); **the barcode's grain** (default here: 24 hourly bars from `/series` today, `step=15m` as a follow-up); **where the `make` line lives on the page** (default here: in Act, under the open ask it answers — `packs/make/rules.yml` says the line belongs to other packs' asks, "the moment you have been told you need something made" — never as a freestanding tile, and only when the node sends the sentence).*

---

## 0 · The ground and the pick

*Fable. Paste into Claude Code on `planetai-node`, branch `dashboard-redesign-2026-09`, with `planetai-design` checked out beside it. Changes nothing under `app/`. Produces `docs/design/PICK_2026-09-20.md`, `docs/design/fixtures/node1-2026-09-18.json`, and a baseline table. Half a day.*

Before any file in `app/static/` moves, write down what is being built and measure what is there, so prompt 7 can prove the difference. The 14 Sep prompt asked for three directions and a pick; the pick happened on a sketch instead. Record it as if it had happened in the repo.

**Read first:** `PROMPT_dashboard_redesign_2026-09-14.md` (Phase 0 and the targets), `docs/design/UX_REVIEW_2026-09.md` and `_skeleton.md` if they exist, `app/static/dashboard.js` in full (the section contract, `STAGES`, the grain table, the netmap, the wall), `docs/site/dashboard.md`, `docs/site/wall.md`, `planetai-design/references/planetai-layer.md`, `planetai-design/decisions/design-log.md`, the two sketch notes named above, and `PLANETAI_Dashboard_Data_Contract_2026-09-20.md` — the pick document cites its field for every component and copies its gap list (A–E) as the node-side work the pack owes.

**The work.** (1) Take the fixture: `planetai snapshot` against node #1 over the tailnet read-only (or the NAS dump if the API still binds loopback), commit it as `docs/design/fixtures/node1-2026-09-18.json`, and confirm `?fixture=node1-2026-09-18` renders the shipped page from it with the pill saying `cached`; write `grain_table[8].home` from that fixture into the pick document as *the* res-8 cell of node #1 — the site review of 20 Sep (R10 §7) found two on one page (`8895a4c843fffff` from the registry's point, `8895a4c86bfffff` from the plan's centre) and the node's own wire is the arbiter, so the pick names one and says which the other is; take the snapshot on **v0.66** or later, so it carries the `make` switch in `/settings` and no facility rows unless the keeper turned the pack on. (2) Run the 14 Sep Phase 0 measurement on that fixture at 390 and 1440, Wall at 1920: T1–T7 and T9, one table, file:line for each miss; count the card grammars. (3) Write `docs/design/PICK_2026-09-20.md`: two sentences per view saying what the sketch shows and which live section it replaces or keeps (use the sketch's own vocabulary — grain rail, lead, ground panel, stages, digest, learn marks, satellite loop, grid, dial); the decisions list above with the two defaults marked *default until Tomas says otherwise*; the component inventory the renderer will need (rail, ruler, lead, pictogram, meter, stamp, ground, stages, matrix, series, unitrow, ledger, barcode, funnel, rings, care, digest, asking, loop, grid, dial, order, field, sections) mapped onto the four card kinds of T3 — say which are composites of readout · stack · series · row and flag any that is not, because a fifth kind is a STOP; the motion inventory from the 20 Sep note with the three new tokens. (4) Add a design-log line R12: the plate on paper with the dark register as a keeper's choice; R13: the grain rail as the top instrument; R14: the Stack's mini variant is the bracket meter.

**Stop** when the fixture renders, the table is filled, and the pick document is written. Do not touch `app/static/`. Ask Tomas only if the fixture cannot be taken from node #1 by any read-only route.

---

## 1 · The skeleton: views, stages, modes, and a page that reflows

*Opus. Touches `app/static/index.html`, `dashboard.js`, `dashboard.css`, `app/settings.py` (one key), `tools/check_ui.py`, `tests/visual/measure.mjs`, `tests/test_dashboard.py`, `docs/GUI.md`. Two to three days. Lands nothing visible beyond the tab order and the mode switch; every section still draws as it does today.*

The shipped page has views, stages and a section contract. It does not have a mode, its tab order is wrong, and at 390 px it overflows. Give it the bones the sketch stands on before any surface is redrawn.

**Read first:** the 14 Sep prompt §2.3 (the renderer's named parts) and §2.7 (gates), `dashboard.js` (`PAI.register`, `STAGES`, `layout`, `render`, the view switch, Arrange), `dashboard.css`, `app/settings.py` (`UI_LAYOUT`), `tests/visual/` if present, `docs/design/PICK_2026-09-20.md`.

**Hard rules.** Tab order becomes Now · Historical · Network · Wall · Arrange · Set up, and `#hash` routes do not change. A new `PUBLIC` setting `UI_MODE` with values `simple | advanced | learn`, default `advanced`, live within 20 seconds like every setting, switchable from a segmented control in the header beside the register switch (paper · dark, which sets `data-theme` on `<html>` exactly as the wall does). The section contract gains one optional field, `level: 'simple' | 'advanced'` (default `advanced`); the renderer hides `advanced` sections in simple mode and draws the **digest** in their place — one sentence per stage, each sentence computed by the node and carried in `/issues` as `digest[stage]`, never composed in the browser — this is **gap A** of the data contract, the first node-side addition the pack makes. Learn mode renders exactly the advanced page plus marks; the marks come in prompt 5, so here `learn` behaves as `advanced` and the segmented control already shows three options. The page is fluid to 1280 px with breakpoints at 1180, 860 and 520; every grid track that holds content is `minmax(0,1fr)`, every grid child `min-width:0`, the matrix scrolls inside its own box; a `measure.mjs` assertion fails `make test` if `document.documentElement.scrollWidth` exceeds the viewport at 390, 768 or 1440 on any view, any mode, either register.

**Commits.** (1) Tab order and the view switch; the register switch; `docs/GUI.md` table. (2) `UI_MODE` in `settings.py` with help text; the segmented control; `level` in the contract; the digest slot with a `/issues.digest` reader and an honest line when the node has not sent one yet (the node side is one commit in `app/issues/` — five sentences from data already in the bundle; STOP and show the templates before writing them). (3) The fluid layout and the overflow gate; the page asserts `schema === 'issues-v0'` on every `/issues` body (v0.63) and prints one sentence, never a blank, on an unknown version; `check_ui.py` learns that a `text-transform: uppercase` rule may not apply to any element whose text contains `µ` (the `MG/M³` bug, seen again on the sketch's wall foot). (4) Tests: mode switch persists per browser and follows the setting; the digest draws in simple; overflow gate red on a deliberately wide fixture, then green.

**Stop** when four commits are in and gates are green. STOP before writing the digest templates.

---

## 2 · The grain rail and the lead

*Opus. Touches `dashboard.js`, `dashboard.css`, `signs.svg` (pictograms), `planetai-theme.css` via `planetai-design/references/planetai-layer.md` (two tokens), `app/static/fonts/` (the mono path), `docs/site/dashboard.md` (one sentence), `tests/visual/`. Three days.*

The first screen. The dial becomes the top instrument, the lead loses its paragraph and its ask strip and gains a pictogram and bracket meters, and the ground panel says what a map costs before you pay it.

**Read first:** `dashboard.js` — the dial and grain table, the lead (`hero`, the why line, the ask strip, the grain line), the ground panel and its three bases, `MAP_TILES`; `docs/site/dashboard.md` §The lead and §The ground; `PLANETAI_Docs_Site_2026-09-16.md` finding 6 (JetBrains Mono `@font-face` 404s on the dashboard); the sketch's Now view.

**Hard rules.** *The rail:* eleven stops (res 2–12, `geometry.grain_table[]`) with `edge_m`; three zones drawn as texture, not hue — dotted where `may_leave`, plain where it stays, struck where `finer_than_published`; the one-cell-here line reads `area_m2`, the counts read `occupied`, `in_my_cell`, `mine_in_my_cell` — all on the wire since v0.57, nothing computed in the browser; the picked stop inverted with arrows; under it a logarithmic rule from 10 m to 200 km with a tick per stop, hidden below 860 px; the key names the two boundaries and says they are settings. The docs sentence "shaded in the cells blue" in `dashboard.md` changes to describe the texture — unless Tomas reverses the default, in which case the rail uses `--cells` at .16 and the docs stay; ask once in the first message and proceed on the default. *The lead:* the kicker is the live sentence; the monument numeral is set in the mono at 132 px on desktop behind a new token `--num-face` (default `var(--mono)`, so Funnel is one line away); a pixel-grid pictogram per issue, drawn once on a 15 × 11 grid (air, heat, land, coast) as `pix-<issue>` symbols in `signs.svg`, never redrawn per reading; the stack as **bracket meters** — `ROOM [▮▮▮▮▮▮▯▯▯▯▯▯▯▯▯▯] 6` in the mono, sixteen cells, one scale for the four distances, the line as a red tick at the same x on every row, an empty distance saying why in words; the four distances take their words from `/issues.labels[locale]` and nowhere else — today those are room · yard · ring · region while the sentences say the wall outside · the street · the model, so the first commit of this prompt changes `LABEL_WORDS` in `app/issues/__init__.py` to the sentence nouns in three languages (the default decision above; ask once, proceed) and the page never carries a distance word of its own; the two paragraphs that overlap on the shipped page are gone, their content is in the why line and in Decide, and the why line states the v0.59 rule (state first, then what moved most in three hours, then the household's order) from the node's own three-language string; the ask strip is gone from the lead and lives in Act, with one line in the stamp saying `1 ask open · #294 · in 3 Act`. *The ground panel:* the three bases stay; the request count moves into the tab bar so the price of `sat` and `osm` is visible before a press; the offline plan leads. *Fonts:* fix the mono `@font-face` path so the numeral is actually JetBrains Mono on a node.

**Commits.** (0) `LABEL_WORDS` → sentence nouns, with the locale test. (1) The rail and the ruler as one component (`rail`, kind: row), fed from `grain_table`; the zones from its two booleans. (2) The lead: token, pictograms, meters, stamp line; remove the ask strip and the two paragraphs; `data-num`/`data-cmp` on every numeral (T4). (3) The ground panel's tab bar and request count. (4) The font path; `docs/site/dashboard.md` sentence; `signs.svg` contact sheet regenerated at 12 px with the four pictograms and a note that they are hero-size only.

**Stop** when four commits are in, T1 and T4 pass on the fixture at 390 and 1440, and the shots are under `docs/design/shots/redesign/lead-*`. STOP if the bracket meter cannot be drawn as text (a font fallback that lacks `▮▯`) — then it is an SVG row of sixteen squares, same anatomy.

---

## 3 · The four stages on Now

*Opus. Touches `dashboard.js`, `dashboard.css`, `signs.svg` (five signs), `app/issues/` (the ledger and the funnel in `/issues`), `tests/`. Three to four days.*

Below the lead the page runs in loop order. Every section keeps its registration; what changes is what each draws and how the ledgers the live page already keeps get a home.

**Read first:** `PLANETAI_Dashboard_Data_Contract_2026-09-20.md` §3 (the row for every section below) and §4 (gaps B, C, D, F); `dashboard.js` every registered section and its `notes()`; `app/issues/engine.py` (`compute`, `_asks_ledger`, `_provenance`); `app/index.py` (`rho` — today `{window_days, days_ago, alerts_act, acted, rho, median_minutes}` and nothing else); `app/main.py` `/series` (hourly only); `schema.py` (`CLOSED_STAGES`: acknowledged · acted · measured); `packs/make/adapter.py` (`ask_line`, `CAPABILITY_WORDS`, the `facility` rows it writes and why `local=False`), `packs/make/rules.yml` (no rules, by design — read its comment), `app/report.py` (the one place that calls `ask_line` today); the sketch's stages 1–4; `PLANETAI_rho_Annex_HalfPage_2026-09-12.md` for the two ρ.

**Hard rules.** Four card kinds and no fifth; every numeral beside its comparison; state in weight never hue; red once on a numeral over a line. *Observe:* the matrix (issues down, room · outside · street · model · line across; empty cells say why) as a `stack`; the day as two `series` with the line dashed red and hours-over marked under the axis, a missing distance absent and named in the legend; sources as Isotype unit rows (`row`): own station, street faint, satellite orange, built and trees of 20, houses one sign per 250; the request ledger promoted from the ground's foot to a full-width `row` section titled *What this page asked of the world*; the barcode as a `series` — **24 hourly bars from `/series` today** (there is no 15-minute route; that is **gap B**, a one-line follow-up adding `step=15m` from `readings`, not this prompt's), the caption saying the grain honestly. *Decide:* the grain that holds (the grain line as a readout plus the four-grain row — `occupied` at res 7–10 from `grain_table`, real data, the sketch's `example` stamp comes off), what leaves (the two boundaries as sentences from settings), the sentence template. *Act:* the ledger from `/issues.asks` (`acts[]`, `actions[]`, `levels`) with level in weight, the ring open or closed by `CLOSED_STAGES`, the green button only on an open ask, the *current* test from `alerts.md` applied; and one line for the household's own capacity where the node has it — the `xiaomi-air` purifier's filter life is the first (v0.61), drawn as a row with its own sign, never as a sensor tile; and **where to go** (v0.65): the fab labs the `make` pack stored arrive on `/sensors` as rows with `kind === 'facility'` (`name`, `meta.distance_km`, `meta.capabilities`, `meta.url`, `meta.snapshot`, `meta.fetched`, `meta.attribution`) and nowhere else — they are absent from `/issues.stations` because `stats` is a view over readings and a place has none. The page draws them as one `row` under the open ask they answer: name · km in the mono · up to three capabilities in the reader's language · the snapshot filename as the date · the word `cached`, since a monthly archive is neither live nor partial; the **sentence** is not composed in the browser — today it exists only in `app/report.py` via `packs/make/adapter.ask_line()`, so this prompt adds `where{loc}` (nullable) to `/issues.asks` in one node-side commit that calls `ask_line` on the stored rows and never re-derives a distance (**gap F**), and until that lands the row carries the facts and no sentence. When `MAKE_ENABLED=0` (the v0.66 default) the row does not exist and the section prints one honest line naming the switch and the reason — the directory's licence covers the Foundation, not this keeper — from the node's own `/settings` help text, never the page's paraphrase. The machine names come from the node in the reader's language (`CAPABILITY_WORDS` en · id · es); an unknown token is shown as itself. A facility is never a station: the wire figure, the unit rows and every count keep their `kind === 'sensor' | 'model'` filters (they do today, `dashboard.js` ~2300) and a test proves a fixture with three facility rows changes no count on Observe or Network. *Measure:* the funnel with the node's stage names — asked · acknowledged · acted · measured — and median latencies between consecutive stages; **the node does not compute this yet (gap C)**: this prompt adds `funnel` and `latency_minutes` to `/rho` from `alerts × actions` in one commit that does not touch ρ's definition, and until that commit lands the page draws rings, `rho` and `median_minutes` only; the ρ rings counted, never sized; ρ reported and, beside it, the words `observed: not yet` (**gap D**, the review pack's prompt 9); the care-label row of refusals as signs. Then the service strip and the Figures ledger generated from `/issues` provenance.

**Signs.** Five new symbols on the 24-unit rules — `sign-air`, `sign-heat`, `sign-land`, `sign-coast`, `sign-person` — into `signs.svg` after the contact-sheet check at 12 px against every existing sign (the sheet has caught two look-alike pairs before; a pair over 90 % identical is a redraw, not a build fix).

**Commits.** One per stage, the `/rho` funnel commit before Measure, the `asks.where` commit before Act (gap F: `engine._asks_ledger` reads the facility rows and calls `ask_line` per locale; a node with the pack off or no lab in the ring sends `null`), then signs, then the strip and Figures (from `issues[*].provenance[]`, already on the wire). Tests: T3 count is four; `/rho.funnel` counts equal the ledger's; the barcode caption names its grain; T4 and T5 gates green; a fixture with no `rho_observed` draws the words, not a blank; a fixture with no open ask draws no button; a fixture with `MAKE_ENABLED=0` draws no facility row and the honest line; a fixture with facility rows and no `asks.where` draws the row without a sentence; three facility rows change no count elsewhere.

**Stop** when seven commits are in and gates are green. STOP before adding any field to `/issues` that a section wants and the node does not compute yet — other than the two named here (`/rho.funnel`, `asks.where`) — list them and wait.

---

## 4 · The other five tabs

*Opus. Touches `dashboard.js`, `dashboard.css`, the `earth` pack's `frames` output path, `docs/site/wall.md` (one paragraph), `tests/visual/`. Four to five days. Can split into two sessions at Wall.*

**Read first:** `dashboard.js` — Historical (`earth`, `trust` sections), Network (`netmap`, `reticulum`, `meshtastic`, hardware), the wall (`#wall`, its dial, grid, fragments, foot), Arrange, Set up (nine groups, the section list); `docs/site/wall.md`; the `earth` pack (`frames`, the Sentinel medians, credits); the sketch's five tabs.

**Historical.** The satellite loop leads: one frame a year, held four seconds (`--motion-satellite-year`), no tween; the node's own record drawn as a raster clipped to the res-8 cell — built in ink, trees in green, changed-since-last-frame in orange — over the Sentinel-2 median for that year when the `earth` pack has one on disk, a marked placeholder when it does not; play/pause, a year strip that holds a year on press, the three figures for the frame, both credit lines. Beside it the four annual medians (2016 · 2019 · 2022 · 2025) as frames with the `partial` pill and the existing credit line, from `/earth`, never a live tile; the Isotype year rows lighting in step with the loop **only if `/earth` carries per-year built and tree fractions — read it first; if it carries only the embeddings' years and the change record, the rows are omitted and recorded, not stamped `example`**; reach per source (**gap E**: three numbers from the oldest `readings_1h` bucket per kind — one small node-side readout, or omit the panel); the trust findings as `row`s beside the instruments they doubt; the barcode grown into a year of daily means. **Network.** The wire figure kept as it is (it is right: ink arrives, blue leaves, a dashed wire has no traffic, CSS motion only); under it radio (`geometry.radio`: presence and its floor), the mesh (`issues.mesh`, one honest line when there is no radio), the other nodes from `registry.json` as cell signs lit or not, the hardware from `/health`, and a new row the sketch does not have because it did not exist when it was drawn: **what this place could read, and where it could go** — `GET /sources` (v0.61; 217 entries at the v0.65 pin `85a194c`, `adapter` per row since v0.64, `role` and `act_kind` and `license` per row since v0.65) as two lines in one `row`: *217 registered · 14 read, by which code · the rest have no adapter yet* and *8 places to act — 2 directories of fab labs · 5 libraries of open designs · 1 list of pledged cities — 3 openly licensed, 5 not*, each count from the rows themselves (`role` contains `act`; `act_kind` ∈ facility · design · network; `license` text as the node carries it, shown on press), the same truth `planetai sources --json` prints. One thing to know before drawing "read": at pin `85a194c` the `fablabs-io` entry's `adapter` is `null` although `pack:make` reads it, because the registry's CI checks adapter strings against this repo and the pack landed the same day as the pin — the page reads `adapter` and never infers, so the row will say 14 until the registry is re-pinned upstream; write that in the handoff as a re-pin owed, not a page bug. **Wall.** As `wall.md` writes it and the sketch draws it: the whole page dark; exit first in tab order; the nineteen cells as the grid with what is read in each (one station: value and name; several: count and range; the own cell: its own stations; an empty cell drawn and left empty); the variable strip; the headline at three metres; the dial as hexagon stops (current ink, may-leave `--cells` at .16, finer dashed) stepping every eight seconds, held thirty on a press; the counts; the rings; five fragments then `+N more in Now`; the foot with `stale`, "Answer on Telegram, not here." and the motion caption; the satellite loop as the right-hand object when the viewport is wide enough (R6), else omitted and recorded. **Arrange.** The sections by stage with ▲ ▼ hide restore, Default and Done, the lead, rail and ground marked fixed, keyboard for everything, saved as `UI_LAYOUT`. **Set up.** The nine groups, the Sources group now backed by `/sources` — each entry's `role`, `adapter` and `license` in the row grammar, so a keeper can see that a source is an act source and what its licence says before anything reads it; the Packs group shows `make` with its five keys (`MAKE_ENABLED` first, its help text carrying the licence sentence from `pack.yaml`, then `MAKE_RADIUS_KM` · `MAKE_SOURCE` · `MAKE_SNAPSHOT` · `MAKE_REFRESH_DAYS`) and the pack's own `doctor` line (labs in the ring, snapshot they came from) when it is on; the Issues pane as a draggable order with the keyboard alternative, saved as `NODE_ISSUES`, one sentence on what reordering does; every field in one row grammar — label · help · value · default · when it takes effect · where it comes from (`.env` or `settings`); secrets masked and never echoed; the registered-section list with whether each is drawing or its honest line.

**Commits.** Historical (two: the loop; the rest), Network, Wall, Arrange, Set up. Tests: the loop stands on the latest frame under reduced motion and pauses when the view is not Historical; the wall renders the refused page in the dark register at `SHARE_LEVEL=off`; T7 on the wall at 1920; every field row has all six parts on the fixture.

**Stop** when the tabs render on the fixture at 390 and 1440 (Wall at 1920) and shots are under `docs/design/shots/redesign/`. STOP before touching the `earth` pack's outputs beyond reading them; if the Sentinel medians are not on disk on node #1, draw the placeholders and say so in the handoff.

---

## 5 · Learn mode

*Opus. Touches `tools/build_learn.py` (new), `app/static/learn.json` (generated, committed), `dashboard.js`, `dashboard.css`, `tools/check_ui.py`, `Makefile`, `docs/site/dashboard.md` §Modes (new), `tests/`. Two days.*

The third mode is the tester guide folded into the page: a question mark floating at each part, a panel that quotes the node's own documentation for it, names the page, links it, and walks to the next. Seventeen marks on Now in the sketch; the sources are in the 18 Sep sketch note's table.

**Read first:** `docs/site/*.md` (dashboard, concepts, sharing, alerts, rho, how-it-works, wall), `tools/build_docs.py` (how pages and anchors are named), the section contract, the sketch's learn panel and its `L` table (keys, quotes, sources).

**Hard rules.** The node does not serve `docs/site`; the build does. So `tools/build_learn.py` extracts the marks at build time into `app/static/learn.json` — for each key: `title`, `quote` (a verbatim substring of one `docs/site` page, ≤ 60 words), `more` (one sentence written for the mark, plainly labelled as the page's own words, not the docs'), `page`, `anchor`, `url` (`https://planetai.fab.city/docs/<page>/#<anchor>`) — and `make docs` runs it, so a docs edit that breaks a quote breaks the build. The gate: every `quote` must still be found verbatim in its source page; every mark a section declares must exist in `learn.json`; no key unused. Sections declare marks through the contract: `learn: ['states','cards']` on registration, drawn by the renderer as a `.q` button at the component's top-right (`data-lv` respected: a simple-mode section carries none). The lead, rail, ground, stages strip and stamp carry their own keys. The panel opens inside the page, quotes, shows `from docs/site/<page>.md · <section>`, links out, has previous/next and Escape; on a phone it takes the full viewport. The marks float 4 px on a 3.2 s ease (`--motion-mark-float`), transform only, zero under reduced motion. The learn bar under the header says where the marks come from and offers *walk the page*. Links open the public docs when online; the quote is always inline, so the mode works on a LAN with no route out.

**Commits.** (1) `build_learn.py`, the seventeen entries from the sketch's table (fix any quote that is not verbatim — the sketch trimmed some with ellipses; the gate does not allow that, so choose the exact span), the Makefile hook, the gate. (2) The contract field, the marks, the panel, the bar. (3) `docs/site/dashboard.md` gains §Modes (simple · advanced · learn) in the docs' voice; a test that the gate fails when a quote is edited out of its page.

**Stop** when three commits are in and `make docs && make lint && make test` are green. STOP if any of the seventeen quotes has no exact span in the current docs — list them for Tomas rather than paraphrase.

---

## 6 · Motion, the loading state, and the layer as a DESIGN.md

*Opus for the code; Fable for `docs/site/design.md`. Touches `planetai-design/references/planetai-layer.md` (three tokens) and the regenerated `planetai-theme.css`, `dashboard.js` (the `asking` component and the landing), `dashboard.css`, `tools/check_ui.py`, `docs/site/design.md` (new), `ARCHITECTURE.md` (one sentence), `tests/`. Two to three days.*

Every motion on the page is named after its datum; three are new; one is the loading state Tomas approved on 20 Sep. Then the layer itself is published in the form agents read.

**Read first:** `planetai-layer.md` (the motion tokens and their OPEN notes), `planetai-theme.css` (the reduce-motion block), `dashboard.js` (`snapshot()`, the "Asking the node…" state, the netmap's CSS motion and its `notes()` about `<animate>`), the 20 Sep motion note and its inventory, the sketch's `asking` component and its canvas code.

**Hard rules.** Three tokens into the layer's table, then regenerate: `--motion-asking: until-data` (recorded as a word, since it has no duration), `--motion-meter-fill: 40ms`, `--motion-mark-float: 3.2s`. Transform and opacity only; one `requestAnimationFrame` loop per page, owned by a tiny scheduler the components subscribe to, paused on `visibilitychange` and by `IntersectionObserver` when the element is off-screen; every duration read from a token; all to zero under reduced motion in the one existing block; no SVG `<animate>` anywhere (gate). *The asking state* replaces the sentence alone at first paint, on ↻ *ask the node again* in the header, and on reconnect after the pill has dropped `live` (v0.58's silence) — **never on a poll**: a poll (v0.58, `POLL_SECONDS`) re-fetches `/issues`, `/health`, `/rho`, re-binds and redraws, and the only motion it may cause is `reading-fade` on the numerals that changed; it never re-fetches tiles, the plan or a satellite year, and the request ledger must not tick on it: one canvas, ≤ 600 glyphs from `0–9 a–f` on a sphere turning at 0.35 rad/s inside the node's res-8 cell in `--cells`, two labels tethered from the frame corners naming the endpoint being read, a spec block bottom-left (res, edge, area, glyph count, share level); beside it the reads the page makes as rings that close when they answer, with latency; the numeral holds `----` until `/issues` lands, then lands in `--motion-reading-fade` and pulses once; the meters fill a cell per `--motion-meter-fill`; the globe settles into the cell plane over 900 ms and the overlay leaves. DPR capped at 2; the canvas is sized to its box, never the viewport; under reduced motion one still frame and the reads land at once. Budget: main-thread work under 4 ms per frame on a 2015 MacBook Pro (node #2's machine) — measure with the Performance panel on the Lima VM and write the number in the handoff.

**The DESIGN.md.** `docs/site/design.md`: the programme layer written in the form styles.refero.design publishes — colours with their one meaning each, type roles, spacing, the four card kinds, the signs, the motion table with datums, the refusals — in the docs' voice, sourced from `planetai-layer.md` and `design-log.md`, nothing invented; linked from the docs nav under a new *Design* group; `llms.txt` points at it; the site review of 20 Sep (R10 §4) has planetai.fab.city link this file once it exists and the R11 Refero pass on the programme page uses `planetai-layer.md` as its lock in the meantime — so this file must say nothing the layer does not, or the two surfaces drift. `ARCHITECTURE.md` §7 gains one sentence: the node's page is three static files and stays so; planetai.fab.city may use a framework; the two never share code.

**Commits.** (1) Tokens and regeneration; the scheduler; the `<animate>` gate. (2) The `asking` component and the landing motions; tests that it stops on `/issues`, that reduced motion draws one frame, that the canvas is absent from the DOM once data is in. (3) `design.md`, nav, `llms.txt`, the ARCHITECTURE sentence.

**Stop** when three commits are in and gates are green. STOP if the frame budget cannot be met at 600 glyphs — halve the count and record it, do not add a worker.

---

## 7 · Prove, hand off, hold

*Opus. Touches `tests/visual/`, `docs/design/shots/redesign/`, `docs/HANDOFF_dashboard_redesign.md`, `CHANGELOG.md`, `docs/GUI.md`, `docs/BETA_TESTER_GUIDE.md`. One day. The tag is Tomas's.*

**The work.** Render every view (six) × every mode (three, Now only for simple/learn) × both registers at 390, 768, 1440 and the wall at 1920 dark, populated, empty and refused, from the committed fixture, with Playwright on the Lima VM, JPEG, one device pixel; keep them as baselines. Run `measure.mjs` and fill the T1–T9 table: shipped baseline (prompt 0) beside the built page. Run the third-party `water` pack fixture from the 14 Sep prompt (1.4) and prove T8 still holds under the new sections. Write the handoff: what shipped, the component · slot · anatomy tables, the motion inventory with measured frame cost, what is unverified (a real wall in a real room; native `id` and `es` readers; the Sentinel medians on node #1; node #2's screen), the exact `planetai update` the testers run, the URLs Tomas opens on the VM with and without the fixture, and the two sentences he sends Lucas and Vivanco. `CHANGELOG.md` in the testers' voice: the page now leads with the grain and the issue that matters; every number sits beside what it is compared to; three modes, the third one teaches; paper by day, dark on the wall; the page shows you what it asked of the world while you looked; and how to change the order of issues. `docs/GUI.md` rewritten for the household in a page.

**Stop** when the table is filled, the shots are in, the handoff is written, and `make lint && make test` are green. Do not open a PR to `main`; Tomas reads the branch. Do not tag; when he says so, `tools/release.sh` cuts the next tag (v0.67 at the time of writing; v0.65 and v0.66 went out on 20 Sep while the pack was being written, the second correcting the first's claim about the `make` switch within hours — the changelog's own standard for a release note) with the changelog section it requires, and per `docs/NEXT_RELEASE.md` rule 3 `app/static/*` ships alone in it — no pack rides along, and the three node-side commits this pack makes (`/issues.digest`, `/rho.funnel`, `asks.where`) go in a release of their own before the page's, so a tester's node has the fields the day the page asks for them.

---

*Order: 0 · 1 · 2 · 3 · 4 · 5 · 6 · 7. Prompts 2 and 3 can run in the same session if context allows; 4 splits at Wall; 5 and 6 are independent of each other once 1 has landed. Every session ends with the three lines the repo expects — ✅ done · ⚠ open · ⛔ needs Tomas — and, if context passes half, writes its handoff first and compacts.*