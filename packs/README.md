Drop a pack folder here. Each pack is a directory with a `pack.yaml` and any of `rules.yml`, `cells.yml`, `adapter.py`.
See `../docs/PACKS.md`. Nothing in this folder is loaded until it has a `pack.yaml`.
