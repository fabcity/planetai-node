# Digest and insight rules. They read readings_1h (hourly means, kept for the whole history) rather than stats
# (24h rolling), because agreement is a question about days, not minutes. Copy this folder to change cadence:
# the digest's cooldown is its interval.

# ---------------------------------------------------------------- the state of the air, three ways
# Not an alert any more. Until v0.37 this fired every three hours at info level, which meant it reached a phone
# only at ALERT_LEVEL=info and was a third schedule when it did. `contributes: report` puts its numbers in every
# report instead, under `digest` in the bundle, so nothing it computes is lost and nothing it computes interrupts.
- id: digest
  contributes: report
  sql: |
    WITH now_in AS (
      SELECT round(avg(mean_1h)::numeric) AS v, count(*) AS n FROM stats
      WHERE local AND indoor AND metric = 'pm25' AND mean_1h IS NOT NULL),
    local_out AS (
      SELECT avg(mean_1h) AS v, count(*) AS n FROM stats
      WHERE local AND NOT indoor AND metric = 'pm25' AND mean_1h IS NOT NULL AND silent_minutes < 120),
    near_out AS (
      SELECT avg(v) AS v, count(*) AS n FROM (
        SELECT mean_1h AS v FROM stats
        WHERE NOT local AND NOT indoor AND kind = 'sensor' AND metric = 'pm25' AND mean_1h IS NOT NULL AND silent_minutes < 120 AND lat IS NOT NULL
        ORDER BY power(lat - current_setting('planetai.lat')::float, 2)
               + power((lon - current_setting('planetai.lon')::float) * cos(radians(current_setting('planetai.lat')::float)), 2)
        LIMIT 3) x),
    -- your own outdoor sensors if you have any; otherwise the three nearest public references
    now_out AS (
      SELECT round(coalesce(lo.v, nr.v)::numeric) AS v, CASE WHEN lo.n > 0 THEN lo.n ELSE nr.n END AS n
      FROM local_out lo, near_out nr),
    model AS (
      SELECT round(value::numeric) AS v FROM observations WHERE sensor_id = 'cams-point' AND metric = 'pm25_model'),
    day_in AS (
      SELECT round(avg(mean_24h)::numeric) AS v, round(max(mean_24h)::numeric) AS peak FROM stats
      WHERE local AND indoor AND metric = 'pm25'),
    trend AS (
      SELECT CASE WHEN a.v > b.v + 3 THEN 'rising' WHEN a.v < b.v - 3 THEN 'falling' ELSE 'steady' END AS t
      FROM (SELECT avg(mean) AS v FROM readings_1h r JOIN sensors s USING (sensor_id)
            WHERE s.local AND s.indoor AND r.metric = 'pm25' AND r.bucket > now() - interval '3 hours') a,
           (SELECT avg(mean) AS v FROM readings_1h r JOIN sensors s USING (sensor_id)
            WHERE s.local AND s.indoor AND r.metric = 'pm25' AND r.bucket BETWEEN now() - interval '6 hours' AND now() - interval '3 hours') b)
    SELECT 'node' AS sensor_id,
           now_in.v AS inside, now_in.n AS n_in, now_out.v AS outside, now_out.n AS n_out, model.v AS modelled,
           day_in.v AS day_mean, day_in.peak AS day_peak, trend.t AS trend
    FROM now_in, now_out, model, day_in, trend
    WHERE now_in.v IS NOT NULL


# ---------------------------------------------------------------- daily: how well do the three layers agree?
- id: agreement
  level: info
  cooldown_minutes: 1440
  sql: |
    WITH h AS (
      SELECT r.bucket,
             avg(r.mean) FILTER (WHERE s.local AND s.indoor)          AS indoor,
             avg(r.mean) FILTER (WHERE NOT s.indoor AND s.kind='sensor') AS outdoor
      FROM readings_1h r JOIN sensors s USING (sensor_id)
      WHERE r.metric = 'pm25' AND r.bucket > now() - interval '7 days'
      GROUP BY r.bucket),
    m AS (
      SELECT date_trunc('hour', ts) AS bucket, avg(value) AS model
      FROM readings WHERE sensor_id = 'cams-point' AND metric = 'pm25_model' AND ts > now() - interval '7 days'
      GROUP BY 1),
    j AS (SELECT h.*, m.model FROM h LEFT JOIN m USING (bucket))
    SELECT 'node' AS sensor_id,
           count(indoor) AS hours,
           round(corr(indoor, outdoor)::numeric, 2)  AS r_in_out,
           round(corr(outdoor, model)::numeric, 2)   AS r_out_model,
           round(corr(indoor, model)::numeric, 2)    AS r_in_model,
           round(avg(outdoor - indoor)::numeric, 1)  AS shield,
           round(avg(model - outdoor)::numeric, 1)   AS model_bias,
           round((100 * (1 - avg(indoor) / nullif(avg(outdoor), 0)))::numeric) AS filtered_pct
    FROM j
    HAVING count(indoor) >= 48 AND count(outdoor) >= 48
  message:
    en: "📊 Your house and the street, this week.\n\nThe air indoors followed the street, as it does in most homes; your building held back a good part of what was outside. The satellite model roughly tracked your street but said nothing useful about your rooms: it sees a district, not a house.\n\nAsk me for the numbers behind this if you want them."
    id: "📊 Rumah Anda dan jalan, minggu ini.\n\nUdara di dalam mengikuti jalan, seperti di kebanyakan rumah; bangunan Anda menahan sebagian besar yang ada di luar. Model satelit kira-kira mengikuti jalan Anda tapi tidak bilang apa pun yang berguna soal ruangan Anda: ia melihat kecamatan, bukan rumah.\n\nTanya angkanya kalau mau."

# ---------------------------------------------------------------- daily: when is the street worst, and does the noise agree?
# Cross-joins `loud`, which is empty without a local outdoor noise sensor — so a node with none gets no alert at
# all, on purpose. See packs/insight/README.md.
- id: rhythm
  level: info
  cooldown_minutes: 1440
  sql: |
    WITH by_hour AS (
      SELECT extract(hour FROM r.bucket AT TIME ZONE current_setting('TimeZone'))::int AS hr,
             avg(r.mean) FILTER (WHERE s.local AND NOT s.indoor AND r.metric = 'pm25')  AS outdoor,
             avg(r.mean) FILTER (WHERE s.local AND s.indoor AND r.metric = 'pm25')      AS indoor,
             avg(r.mean) FILTER (WHERE s.local AND NOT s.indoor AND r.metric = 'noise') AS noise
      FROM readings_1h r JOIN sensors s USING (sensor_id)
      WHERE r.metric IN ('pm25', 'noise') AND r.bucket > now() - interval '7 days' AND s.kind = 'sensor'
      GROUP BY 1),
    worst AS (SELECT hr, outdoor FROM by_hour WHERE outdoor IS NOT NULL ORDER BY outdoor DESC LIMIT 1),
    best  AS (SELECT hr, outdoor FROM by_hour WHERE outdoor IS NOT NULL ORDER BY outdoor ASC  LIMIT 1),
    loud  AS (SELECT hr, noise   FROM by_hour WHERE noise   IS NOT NULL ORDER BY noise   DESC LIMIT 1)
    -- quiet_hr holds the loudest hour, not the quietest. Named for tests/test_shipped.py, which pins this
    -- column name; do not read the name as the value.
    SELECT 'node' AS sensor_id,
           lpad(worst.hr::text, 2, '0') AS worst_hr, round(worst.outdoor::numeric) AS worst_v,
           lpad(best.hr::text, 2, '0')  AS best_hr,
           lpad(loud.hr::text, 2, '0')  AS quiet_hr
    FROM worst, best, loud
  message:
    en: "🕐 Your street has a rhythm.\n\nThis week the outside air was worst around {worst_hr}:00 and cleanest around {best_hr}:00. The street was loudest around {quiet_hr}:00, a different hour entirely. Burning after dark fits that pattern.\n\n👉 If you air the house, {best_hr}:00 is the hour."
    id: "🕐 Jalan Anda punya ritme.\n\nMinggu ini udara luar terburuk sekitar pukul {worst_hr}:00 dan terbersih sekitar pukul {best_hr}:00. Jalan paling ramai sekitar pukul {quiet_hr}:00, jam yang sama sekali berbeda. Pembakaran setelah gelap cocok dengan pola ini.\n\n👉 Kalau ingin membuka rumah, pukul {best_hr}:00 waktunya."
