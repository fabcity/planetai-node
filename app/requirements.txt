fastapi>=0.115,<1
uvicorn>=0.31.1,<1
psycopg[binary]==3.2.*
httpx==0.27.*
pyyaml==6.*
paho-mqtt==2.1.*
mcp>=2.0
# the hero's ground: this node's own H3 cell, drawn from NODE_LAT/NODE_LON at request time (app/ground.py).
# Wheels only, no build; a node with no route out still draws its own cell.
h3==4.*
